# Dependências de desenvolvimento.
#
# Ficam separadas do requirements.txt de propósito: a imagem Docker não precisa
# da suíte de testes, e mantê-la fora reduz o tamanho da imagem de produção.
#
# Instalação:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest==8.3.3
